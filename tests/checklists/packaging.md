# Packaging

- macOS release build creates `RGCS.app`.
- Ubuntu release build runs on a clean supported Ubuntu image.
- AppImage launches without installed Qt development packages.
- App data database is created in the user app data directory.
- Telemetry log folder is configurable and survives restart.
